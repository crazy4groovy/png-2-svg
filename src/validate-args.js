export default {
  clobberFile: v => v !== 'false',
  colors: Number,
  combineLines: v => v === 'true',
  decimalPlaces: v => v !== 'false',
  scale: Number,
  smooth: Number,
  tolerance: Number,
  vibrant: v => v !== 'false',
  writeFileWithTag: String
}
