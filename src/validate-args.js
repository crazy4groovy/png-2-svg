export default {
  clobberFile: v => v !== 'false',
  colors: Number,
  combineLines: v => v === 'true',
  scale: Number,
  smooth: Number,
  smoothDecimalPlaces: Number,
  tolerance: Number,
  vibrant: v => v !== 'false',
  writeFileWithTag: String
}
